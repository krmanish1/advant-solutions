/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // domains: ["images.unsplash.com"],
        domains: ["img.freepik.com", "cdn.prod.website-files.com"],
    },
};

export default nextConfig;
